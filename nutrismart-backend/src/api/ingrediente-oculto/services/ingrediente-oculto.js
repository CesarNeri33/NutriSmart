'use strict';

/**
 * ingrediente-oculto service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::ingrediente-oculto.ingrediente-oculto');
